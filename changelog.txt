CLOUD BASED
===============================================================

A SOFDESG and LBYCPD2 project, created by Cheng Roa and Tejada

===============================================================

Version 0.1 (March 18, 2026)
- Project Architecture & Interface: Created core project files (HTML, CSS, JS) and established the working interface.
- Project Architecture & Interface: App.js: Built the main application files, implemented global state management, and created seamless tab navigation between app screens.
- Project Architecture & Interface: UI/UX Styling: Fully modularized index.css using modern design principles, flexbox layouts, CSS variables, and clean interactive elements (hover states, custom UI buttons).
- Components & Features: InventoryTable.js: Implemented a dynamic interactive table displaying items and properties. Features include a management toolbar with Search/Add tools, individual/bulk row checkbox selection, and responsive state changes.
- Components & Features: SupplierTable.js: Replicated and adapted core table functionalities to display suppliers and contact information, complete with unique column sizing and its own interactive toolbar.
- Components & Features: UserSettings.js: Implemented a dedicated Settings dashboard populated with system configurations, featuring a working application-wide Light/Dark Mode theme switcher.

Version 0.2 (March 18, 2026)
- Project Architecture & Interface: Re-architected project tree: separated logic files into /App Logic/ folder and CSS into /Resources/ for modular cleanliness.
- Project Architecture & Interface: Implemented real-time dynamic search bars across both Inventory and Supplier tables, filtering across multiple columns simultaneously on every keystroke.
- Project Architecture & Interface: Added smart visual safety-locks preventing erroneous multiple "Edits" when checking off bulk items.
- Project Architecture & Interface: Upgraded entire table rows to be seamlessly clickable instead of targeting just the checkbox.
- Components & Features: Prompt.js: Developed a reusable, animated floating modal overlay (pop-up box). It elegantly dims the main background to safely handle all specific interaction forms (e.g., Add Item, Edit Supplier) anywhere in the application.
- Components & Features: Prompt Modals: Fully implemented detailed Add/Edit mock forms matching Figma layout, equipped with native dropdowns and linked dynamically to global supplier data.
- Components & Features: UserSettings.js: Added "Global Stock Threshold" module (with Enable/Disable disable logic), providing real-time bounding limit validation for computing inventory statuses dynamically across the app.
- Components & Features: Supplier Details: Added a dedicated "Supplier Details" contact card view in the Prompt overlay, dynamically opened directly by clicking on any linked supplier name in the Inventory Table.

Version 0.2.1 (March 18, 2026)
- Git & Project Configuration: Removed spaces from `App Logic` folder (`AppLogic`) and updated paths in `index.html` to prevent URL encoding issues and cross-platform sync problems with Git and GitHub Pages.
- Git & Project Configuration: Added `.gitignore` to safely prevent OS-level items like `.DS_Store` and generic config files from polluting the master branch.
- Git & Project Configuration: Added `.gitattributes` to explicitly force LF line endings (`text=auto`) to avoid painful `CRLF` versus `LF` conversion issues on Windows when cloning or pushing.
- Mobile UI Fix: UserSettings.js: Refactored hardcoded inline styles into dynamic CSS classes (`setting-item-inner`, `threshold-controls`) to allow for seamless mobile responsiveness.

Version 0.2.2 (March 18, 2026)
- Mobile & Desktop UI Polish: UserSettings.js: Refactored hardcoded inline styles into dynamic CSS classes to allow for seamless mobile responsiveness, while correctly restoring desktop-only flex gaps for the bounding threshold controls.
- Mobile & Desktop UI Polish: index.css: Added comprehensive `@media` queries for mobile screens (`max-width: 768px`).
- Header & Navigation Restructure: Favicon: Registered project branding `icon.png` as the official website favicon.
- Header & Navigation Restructure: Topmost Brand Bar: Created a new absolute top-level edge-to-edge header matching the table highlight colors. Moved the "CloudBased" Logo, system tabs ("Inventory", "Suppliers"), and the User Avatar into this unified bar.
- Header & Navigation Restructure: User Profile: Stripped redundant "Administrator" labels to clean up the profile dropdown layout.
- New Features & Functionality: Warehouse Filter Bar: Repurposed the previous sub-navigation space into a horizontal scrollable filter bar. Automatically computes unique Warehouses and renders them as clickable "pill" cards that actively pre-filter the Inventory table data.
- New Features & Functionality: Table "Select All" Headers: Upgraded the main column definition row (`header-row`) in both the Inventory and Supplier tables so that clicking anywhere on the header dynamically selects or deselects all visible items instantly.

Version 0.3 (March 18, 2026)
- Data Persistence & Creation of Backend System: AppDataHandler.js: Implemented a robust data loading system that prioritizes: Session Cache (localStorage) -> Modified Files (settings.json, uoms.json) -> Constant Defaults (defaultsettings.json, defaultuoms.json).
- Data Persistence & Creation of Backend System: Default Logic: Added support for `defaultsettings.json` and `defaultuoms.json` as permanent constant fallbacks for initial application startup.
- Data Persistence & Creation of Backend System: Temporary Files: Created `settings.json` and `uoms.json` to store user-defined overrides, ensuring defaults remain untouched.
- Data Persistence & Creation of Backend System: Local Persistence: Configured the application to automatically record all modifications into temporary browser storage, providing a seamless bridge until a cloud database is established.
- App Logic & CRUD Operations: Inventory Management: Fully implemented Create, Read, Update, and Delete (CRUD) operations for the Inventory table.
- App Logic & CRUD Operations: Supplier Management: Fully implemented Create, Read, Update, and Delete (CRUD) operations for the Supplier table.
- App Logic & CRUD Operations: Prompt.js: Transformed the static Prompt modal into a dynamic, state-driven form handler with controlled inputs and intelligent pre-filling for edit operations.
- App Logic & CRUD Operations: UI Polish: Removed all development console logs and mockup simulation notes to provide a cleaner, production-ready user experience.
- App Logic & CRUD Operations: Selection Logic: Added automatic selection clearing across tables whenever data is modified, ensuring a seamless and error-free bulk action workflow.
- App Logic & CRUD Operations: UX Stability: Fixed React ReferenceErrors (useState/useEffect) caused by script-based Babel scoping.
- App Logic & CRUD Operations: Field Editing: Updated Quantity and Min Level fields to support empty inputs, allowing for easier clearing and precise data entry.
- App Logic & CRUD Operations: Supplier Assignment Fix: Added a dynamic placeholder and improved selection logic to ensure correct mapping of suppliers to inventory items, even when added after the item.
- App Logic & CRUD Operations: Visual Clarity: Updated the Inventory Table to display "N/A" for empty supplier fields, ensuring clear visibility of data gaps.

Version 0.4 (March 19, 2026)
- Cloud Database Integration (Firebase Firestore): Integrated Google Firebase Firestore as the cloud database backend. All inventory, supplier, UOM, and warehouse data is now stored in and retrieved from Firestore, making it accessible across all devices and browsers.
- Cloud Database Integration (Firebase Firestore): index.html: Added Firebase App and Firestore compat SDK scripts (loaded via CDN, no bundler required).
- Cloud Database Integration (Firebase Firestore): AppDataHandler.js: Fully rewrote all fetch and save methods to communicate with Firestore. Settings remain in localStorage as they are device-specific user preferences.
- Cloud Database Integration (Firebase Firestore): Data split: Firestore handles shared application data (inventory, suppliers, UOMs, warehouses); localStorage handles personal settings (theme, threshold) and the Firebase config override.
- Cloud Database Integration (Firebase Firestore): Seeding Logic: On first load, if a Firestore collection (UOMs, warehouses) has no document, the app automatically seeds it from the corresponding default JSON file and writes it to Firestore, so all future reads come from the database.
- AppDataHandler Architecture Overhaul: AppDataHandler.js: Refactored the data loading system so that `defaultsettings.json` is the authoritative source of startup defaults, with localStorage as the primary store for user-modified preferences. Removed `settings.json` from the priority chain entirely.
- AppDataHandler Architecture Overhaul: Removed redundant save-file stubs (`inventory.json`, `suppliers.json`, `uoms.json`, `settings.json`) that were unused now that localStorage handles all runtime storage.
- AppDataHandler Architecture Overhaul: Renamed `warehouses.json` to `defaultwarehouses.json`, aligning it with the established `default*.json` naming convention for constant fallback files.
- AppDataHandler Architecture Overhaul: Replaced the old `fetchData`/`saveData` generic helpers with explicit, self-documenting methods per data type. Each method is now transparent about exactly what it reads and writes.
- AppDataHandler Architecture Overhaul: Added `clearAllData()` utility method that removes all app-scoped localStorage keys (prefixed `cloudbased_tmp_`) without affecting Firestore data or the Firebase config override.
- AppDataHandler Architecture Overhaul: Added `getFirebaseConfig()` and `saveFirebaseConfig()` public methods, enabling the Settings UI to read and update the active Firebase project config at runtime.
- AppDataHandler Architecture Overhaul: Firebase initialization now reads from a user-saved config in localStorage first, falling back to the hardcoded `DEFAULT_CONFIG`. This allows switching Firebase projects without modifying source code.
- Settings Enhancements: UserSettings.js: Added a "Units of Measure (UOM)" settings block allowing users to add or remove UOM entries. Changes are immediately persisted and reflected in the item form dropdowns.
- Settings Enhancements: UserSettings.js: Added a "Reset App Data" settings block with a dedicated button that clears all localStorage entries scoped to this application and reloads the page, reverting to default values.
- Settings Enhancements: UserSettings.js: Added a "Warehouses" settings block, mirroring the UOM settings block. Users can add or remove warehouse locations; changes are immediately saved to Firestore and reflected in the filter bar.
- Settings Enhancements: UserSettings.js: Added a "Database Configuration" settings block displaying all active Firebase config fields (API Key, Project ID, etc.) as editable inputs. "Save & Reload" applies a new config; "Reset to Default" restores the hardcoded values.
- Loading Screen Overhaul: app.js: Replaced the inline loading placeholder (which rendered inside the full app shell) with an early return that shows a clean, bare full-screen loading view. The navigation bar and filter bar no longer appear while data is loading.
- Loading Screen Overhaul: index.html: Added a synchronous inline script in `<head>` that reads the saved theme from localStorage before React renders, eliminating the brief dark/light theme flash on initial page load.
- Loading Screen Overhaul: Loading text simplified to "Loading Resources" with no subtitle.
- UI/UX Polish: index.css: Fixed Settings tab overflow — content now scrolls within the bounding box instead of pushing beyond the viewport.
- UI/UX Polish: index.css: Added `.setting-info` flex constraints to prevent long description text from crowding adjacent buttons in setting rows.

Version 0.4.1 (March 19, 2026)
- UI/UX Polish: index.css: Added scrolling to the prompts. Added display 'flex' for auto stretch, and set 'overflow-y: auto' to automatically add a scrollbar once the prompt is taller than the viewport.

Version 0.4.2 (March 19, 2026)
- Security & Authentication: index.html: Added Firebase Authentication compat SDK to the project headers.
- Security & Authentication: AppDataHandler.js: Implemented automatic anonymous authentication (`signInAnonymously`). This provides a secure layer for Firestore, allowing the transition from open "Test Mode" rules to authenticated-only access (`if request.auth != null`).
- Security & Authentication: Security Patch: Addressed automated secret scanning alerts from GitHub and Google by securing the database logic against unauthorized external access.

Version 0.4.3 (March 19, 2026)
- Security & Data Load Fixes: AppDataHandler.js: Resolved a race condition where data fetches would fire before authentication was established. Implemented a global `authPromise` that all Firestore methods await to ensure database queries only execute once signed in.
- Security & Data Load Fixes: app.js: Wrapped the initial startup sequence in a `try...finally` block to prevent the application from hanging on the "Loading Resources" screen if a network error or authentication delay occurs.

Version 0.5 (March 19, 2026)
- Inventory Transaction Logs: Triple-View Switcher: Seamlessly toggle between Overview (Live Stock), Input Log (Incoming), and Output Log (Outgoing).
- Inventory Transaction Logs: Unified Table Architecture: All views share the same high-density interface with bulk selection, checkbox controls, and smart search.
- Inventory Transaction Logs: Bi-Directional Quantity Sync: Input logs automatically increase inventory (+), while output logs subtract (-), with robust decimal precision.
- Inventory Transaction Logs: Full History Editing: Edit or remove log entries with automatic quantity reversal.
- Inventory Transaction Logs: Location-Aware Logs: The Warehouse filter bar now dynamically filters logs based on the linked item's storage location.
- Advanced Sorting System: Intelligent Sort Button: A new interactive pill-button matching the search bar height.
- Advanced Sorting System: Custom Sort Logic: Alphabetical (A-Z/Z-A), Numerical (High/Low), and Time-based (Newest/Oldest) sorting across all tables.
- Advanced Sorting System: Smart Priority: "Status" sorting places Low Stock items at the top; Warehouse sorting allows prioritizing specific locations.
- Advanced Sorting System: Visual Feedback: The button highlights in accent color and displays the active sort choice for quick reference.
- UI/UX Finalization: "Save Changes" Standardization: Synchronized button styles across all administrative forms for a consistent premium feel.
- UI/UX Finalization: Status Badge Polish: Fixed badge wrapping to maintain a clean pill shape on mobile and high-density screens.
- UI/UX Finalization: Animated Dropdowns: Implemented smooth fade-in animations for the new sorting controls.
- Codebase Architecture & Simplification: Shared Components: Extracted the complex SortButton and ViewSwitcher into a generalized `AppLogic/Shared.js` utility module.
- Codebase Architecture & Simplification: InventoryTable Refactoring: Consolidated the duplicate 100+ line table template structures into a single unified render loop that switches row contents based on activeView.
- Codebase Architecture & Simplification: app.js Simplification: Rewrote the massive `if-else` cascade in `handlePromptConfirm` into a cleanly delineated `switch` statement for improved maintainability.
- Comment Naturalization: Removed overly robotic AI-generated block comments. Replaced rigid structural markers with concise, functional developer notes.
- File System & Coding Style: Unified all JavaScript filenames to `lowerCamelCase` style for project consistency. Updated index.html script references.

Version 0.6 (March 19, 2026)
- ERPNext Integration & Data Exporting: ERPNext Export Tool: Developed a brand new `exportTool.js` module that generates CSV files for ERPNext.
- ERPNext Integration & Data Exporting: 4-Row Header Mapping: Implemented the mandatory ERPNext structure for zero-config auto-mapping.
- ERPNext Integration & Data Exporting: New Record Creation: Configured exports to leave the ID column blank by default for new record auto-creation.
- ERPNext Integration & Data Exporting: Export Modules: Added support for "Item List" and "Stock Reconciliation" DocTypes.
- ERPNext Integration & Data Exporting: Precise Data Encoding: Utilized UTF-8 with BOM for correct Excel display.
- Settings UI Enhancements: Export Block: Integrated a new "Export to ERPNext" section into the User Settings dashboard.
- Settings UI Enhancements: User Context Support: Connected export engine to live application state for most recent data.

Version 0.6.1 (March 20, 2026)
- Database Configuration & Resilience: Default Configuration: Separated hardcoded Firebase config into a standalone `AppData/defaultDatabase.json` file.
- Database Configuration & Resilience: AppDataHandler Lifecycle: Refactored initialization to handle User Settings -> Default Database File.
- Database Configuration & Resilience: Error Handling & Recovery: Implemented robust "Database Not Found" states with clear reason reporting.
- Settings UI & Security Polish: API Key Toggle: Added visibility toggle for masking the Firebase API Key.
- Settings UI & Security Polish: Mobile Layout Fix: Stacking labels and inputs on narrower viewports to prevent overflow.
- Settings UI & Security Polish: Settings Reorganization: Structured dashboard into logical categories (Appearance, Inventory Framework, Data & Connectivity, System Operations).
- Settings UI & Security Polish: User Avatar Dropdown: Sleek menu for "User Settings" and "Database Help".
- Settings UI & Security Polish: Refined Animations: Smooth chevron rotation and scaling effects.

Version 0.6.2 (March 23, 2026)
- Data Integrity & Validation: Robust Validation: Implemented duplicate entry guards for Item IDs, Supplier Name/IDs, and Transaction IDs.
- Data Integrity & Validation: Error Feedback: Persistent notification banner for real-time validation feedback.
- Data Integrity & Validation: Supplier Management: Exposed Supplier ID field for manual record control.
- Component Bug Fixes & Refinements: Prompt Initialization: Resolved log sourcing bugs.
- Component Bug Fixes & Refinements: Logic Coverage: Added initialization support for all transaction types.
- Component Bug Fixes & Refinements: Field Resetting: Auto-cleared validation errors upon form interactions.

Version 0.7.0 (March 23, 2026)
- Authentication & Access Control: Dedicated Login Gateway: Launched `login.html` for secure auth flow isolation.
- Authentication & Access Control: Modern Auth UI: Elegant interface with real-time validation.
- Authentication & Access Control: Multi-User Identity: Profile system with display names and bio pictures.
- Authentication & Access Control: Persistent Session Management: Engineered secure redirects for logged-out users.
- Redesigned User Settings Dashboard: Category-First Layout: Modern modular layout for profile, operations, and info.
- Redesigned User Settings Dashboard: Profile Management: Username updates and bio-picture clearing.
- Redesigned User Settings Dashboard: Credits & Origin: Official branding and resources section.
- Redesigned User Settings Dashboard: Version Tracking: Formalized build info (v0.7.0, March 23, 2026).
- Core Architecture & Shared Logic: Component Abstraction: Refactored redundant UI controls into `sharedComponents.js`.
- Core Architecture & Shared Logic: Form Components: Standardized `FormInput`, `FormSelect`, and `FormButtons`.
- Core Architecture & Shared Logic: Unified Media Engine: Shared client-side image resizing utility.
- Advanced Navigation & Filtering: Dynamic Pill-Style Interface: Consistent premium gill-tab design.
- Advanced Navigation & Filtering: True Multi-Warehouse Filtering: Location pills extended across all logs and sites.
- Snappier UI & Refined Interaction: Snap-to-View Transitions: Instant transitions in favor of opacity fades.
- Snappier UI & Refined Interaction: Catalog Interactivity: Clickable Item List cards for instant editing.
- Snappier UI & Refined Interaction: Remove Image Logic: Simple asset resetting for profile and items.

Version 0.8.0 (March 23, 2026)
- Executive Insights & Dashboard Logic: Integrated Analytical Dashboard with 8 specialized logic blocks.
- Executive Insights & Dashboard Logic: Smart KPI Cards for Items, Low Stock, and Suppliers.
- Executive Insights & Dashboard Logic: Inventory Velocity tracking with specialized tables.
- Executive Insights & Dashboard Logic: Risk Mitigation: Automated flagging for replenishment required.
- Executive Insights & Dashboard Logic: Geographic & Category Distribution: Warehouse and performance visualizations.
- Security & Authentication Refinement: Enhanced Password Recovery with inter-linked flows.
- Security & Authentication Refinement: Manual Credential Updates with current password verification.
- Security & Authentication Refinement: Navigation Fluidity: Centered UI aesthetic and layout centering.
- Security & Authentication Refinement: Logic Patch: Resolved Babel race conditions in login.
- Catalog & Property Enhancements: Product Descriptions field added to Item Master.
- Catalog & Property Enhancements: Metadata Visibility: Updated cards to display descriptions.
- Catalog & Property Enhancements: Prompt Integration: Smart-resized textarea for items.
- Data Management Cleanup: Retired import stub to focus on migration features.

Version 0.9.0 (March 23, 2026)
- Data Migration & Advanced Administration: Power Import Engine supporting SheetJS integration.
- Data Migration & Advanced Administration: Automated Mapping for worksheets to Firestore schema.
- Data Migration & Advanced Administration: Smart Data Transformation: Unique companies as Suppliers.
- Data Migration & Advanced Administration: System Factory Reset (Nuke): High-security clearing challenge.
- Data Migration & Advanced Administration: Universal Scrolling Fix for high-density navigation.
- Data Migration & Advanced Administration: Branding & Identity Sync: decoupled header reflecting company name.

Version 0.10.0 (March 23, 2026)
- UI/UX Branding & Customization: Logo Persistence Engine for real-time asset management.
- UI/UX Branding & Customization: Global Accent Picker for synchronized corporate identity.
- UI/UX Branding & Customization: Unified Brand Header reflecting company unit.
- UI/UX Branding & Customization: Header Visibility Fix for dark theme transparency.
- Inventory Lifecycle Tracking: Restocked? Indicator for Addressing low-stock alerts.
- Inventory Lifecycle Tracking: In-Process (I) Status for in-flight replenishment.
- Inventory Lifecycle Tracking: Import Mapping Logic for replenishment statuses.
- Inventory Lifecycle Tracking: Real-Time Badge UI (Green, Red, Indigo).
- Interactive "Fat-Finger" Accessibility: Full-Row Selection for mobile usability.
- Interactive "Fat-Finger" Accessibility: Master Selection Guard for bulk capture.
- Interactive "Fat-Finger" Accessibility: Standardized Hit Areas with hover feedback.
- Intelligent Dashboard Filtering & AI: Alert Suppression for addressed items.
- Intelligent Dashboard Filtering & AI: InnoAssistant (AI) Cognitive Sync for smarter advice.
- Intelligent Dashboard Filtering & AI: Stock Reconciliation Guard for database resilience.

Version 0.11.0 (March 24, 2026)
- Security & System Resilience: High-Fidelity Password Recovery with custom confirmation dialogs.
- Security & System Resilience: Quota Education: Firebase email quota and spam warnings.
- Security & System Resilience: Stock Threshold Defaults: Disabled by default for new accounts.
- Rebranding & Identity: Predictive Replenish Integration (Renamed from Oracle).
- Rebranding & Identity: InnoAssistant Cognitive Sync (Renamed from Aura AI).
- Rebranding & Identity: Unified Branding: Synchronized documentation and historical logs.

Version 0.11.1 (March 24, 2026)
- Dynamic Branding & Identity: Globally standardized system naming to fall back to "System" instead of "CloudBased" for white-label reliability.
- Dynamic Branding & Identity: Fully integrated the administrator-defined Company Logo and Name into the Login, Loading, and Header screens.
- Dynamic Branding & Identity: Preserved "CloudBased IMS" branding exclusively within the User Settings "About" section as a historical reference.
- InnoAssistant V2 (Neural Upgrade): Massively expanded the natural language synonym library, supporting dozens of new phrasing variations for arrivals, shipments, and queries.
- InnoAssistant V2 (Neural Upgrade): Implemented Context-Aware Error Reporting with specific suggestions based on missing item quantities or unidentified SKUs.
- InnoAssistant V2 (Neural Upgrade): Added Intelligent Property Queries allowing users to ask about item locations, categories, stock levels, and transaction history.
- InnoAssistant V2 (Neural Upgrade): Integrated Smart Date Recognition for "Today" and "Yesterday" keywords in logging commands.
- UI/UX Generalization: Replaced hardcoded placeholder examples (e.g., specific names/emails) with generic professional labels across all management prompts.

Version 0.11.2 (March 24, 2026)
- InnoAssistant Transaction Engine (Fix): Resolved a critical bug in the action handler where background commits would fail to resolve without an active UI modal.
- InnoAssistant (Undo/Reverse): Implemented a new conversational "Undo" capability that recognizes keywords like "reverse that", "mistake", or "wrong entry" to instantly roll back the last transaction and restore stock levels.
- Executive Dashboard Promotion: Upgraded "Critical Replenishment Required" from a hidden tab to the primary Dashboard Overview for improved business intelligence.
- System Logic Sync: Standardized all AI-driven stock warnings to respect global thresholds and MSL account overrides.
- Global Administration: Fixed a labeling bug in the Admin Settings where Replenishment Alerts were incorrectly referred to as a Beta feature.

Version 0.11.3 (March 24, 2026)
- Admin Activity Logs (Persistent Collection): Launched a dedicated activity logging system stored in Firestore that tracks all administrative and user-driven changes across the platform beyond basic transactions.
- CSV Data Export (Activity Logs): Integrated a dynamic CSV export tool for activity logs, supporting filtered data views and proper field escaping for professional spreadsheet compatibility.
- Actor Branding & Normalization: Standardized the default system actor name to "System" instead of "System Import" across all automated transaction and sync logs for a cleaner, prioritized audit trail.
- Multi-User Identity Tracking: Implemented granular session and profile event logging, including manual logins, account signups, password resets, and profile-specific administrative events.
- Security & Auditor Enforcement: Hardened the internal permission layer to ensure that all restricted Auditor actions are blocked and omitted from the log history, preventing unauthorized clutter.
- Administrative Maintenance: Added a "Clear Activity Logs" utility for administrators to reset purely administrative history while preserving the core business transaction records.

Version 0.11.4 (March 25, 2026)
- Security & Access Control: Disabled public user registration (Self-Signup) to preserve the integrity of the private database.
- Auditor Permission Defaulting: Hardened the default security posture for new Auditors. All administrative restrictions are now enabled by default upon account creation or role assignment.
- Session & Migration Logic: Enhanced the session initializer to automatically apply the full restriction set to existing Auditors without explicitly defined permissions.
- Admin Dashboard Improvements: Synchronized the User Management interface to automatically reflect the new high-security defaults for the Auditor role.
- System Maintenance: Updated the platform to Version 0.11.4.

Version 0.12.0 (March 25, 2026)
- Modular Dashboard Grid System: Implemented a fully customizable 2D grid layout for the Overview and Act tabs. Users can now reorder modules using directional controls and toggle between half-width and full-width sizes.
- Admin Dashboard Redesign: Consolidated all system-wide configurations into a new "Global Settings" tab. This includes Branding, Module Visibility, Warehouse Management, and Database Configuration.
- Branding & Identity Customization: 
    - Added a Global Accent Color picker that instantly updates the corporate color scheme across the entire platform.
    - Implemented dynamic browser tab synchronization: the page title and favicon now automatically reflect the Administrator-defined Company Name and Logo.
    - Fixed PNG transparency support by upgrading the image processing engine to preserve alpha channels (WebP/PNG).
- Module Evolution & Warehouse Health: 
    - Renamed "Warehouse Distribution" to Warehouse Health.
    - Re-engineered the Health Score to represent "Stock Integrity" out of 100%, providing a clearer business grade for inventory levels.
- InnoAssistant Layout Fixes: 
    - Resolved a critical UI bug where the AI disclaimer and response areas would be cut off on smaller screens. 
    - Implemented responsive stacking for the assistant interface, ensuring 100% visibility on all desktop resolutions.
- Data Logic & Sync Enhancements:
    - Fixed a bug where the isRestocked flag would stick permanently; the system now automatically resets restock statuses upon any new arrival (Input Log).
    - Improved dashboard data reactivity by ensuring local caches are correctly invalidated when global settings or branding data changes.
- Administrative Experience (UX): 
    - Added "Quick Save" buttons to each major settings block (Branding, Modules, Firebase) for faster configuration without scrolling.
    - Grouped core metrics (Total Items, Low Stock, Suppliers) into a locked, non-resizable unit to preserve the high-level KPI layout.

Version 0.13.0 - March 26, 2026
-------------------------------------------------------
- Hybrid Cloud Architecture:
    - Implemented a unified dual-backend system supporting both independent PocketBase (VPS) and legacy Firebase (cloud) modes.
    - Added a hidden database-mode toggle on the Login screen to switch between environments.
    - Engineered `FirebaseBridge` as a modular adapter to restore legacy auth and data functionality without bloating the main runtime.
- Infrastructure & Deployment:
    - Migrated production to a Linux VPS with Caddy reverse proxying and DuckDNS resolution.
    - Hardened server security by removing UI-based endpoint configuration and moving connectivity to master config files.
    - Configured PocketBase as a `systemd` service with automated routing to port 8090.
- Data Reliability & Cloud Sync:
    - Restored full user, activity log, and stock synchronization for legacy Firebase environments.
    - Synchronized global assets, warehouses, and branding across both database backends.
    - Solved critical null-reference and security-rule crashes by rerouting settings to the correct legacy Firestore collections.
    - Fixed specialized data-type mapping such as invalid dates and migration keys to keep backend switching reliable.
- Infrastructure Migration:
    - Integrated the PocketBase JS SDK as the primary local/cloud database engine.
    - Re-engineered `AppDataHandler` around PocketBase collection methods such as `getList`, `getFullList`, and `create`.
    - Added `assets/data/pocketbase_config.json` as the centralized PocketBase connection source.
- Admin Dashboard & Server Management:
    - Modernized the Activity Logs tab with more defensive date handling to prevent deduplication issues.
    - Standardized branding to fall back to `assets/images/icon.png` for offline-resilient white labeling.

Version 0.13.1 - March 27, 2026
-------------------------------------------------------
- Data Management & Reliability:
    - Fixed a critical cold-start bug where Activity Logs could silently fail to load in the Admin Dashboard.
    - Resolved the `initPromise` bypass in `admin.js` so PocketBase is fully ready before administrative queries run.
    - Improved the merged activity-log view so historical stock records and system audit events render more consistently.
- UI/UX Scaling & Visual Fidelity:
    - Fixed a major SVG rendering bug where icons could shrink, blur, or disappear because `size` props leaked into the DOM.
    - Refactored the shared icon library to consume only safe SVG attributes, improving consistency across the app.
    - Stabilized small action icons such as edit and permission controls so they remain aligned and visible.

Version 0.13.2 - March 27, 2026
-------------------------------------------------------
- PocketBase Schema Alignment:
    - Reworked create, edit, and save flows to match the live PocketBase collections instead of older Firebase-era assumptions.
    - Fixed user management writes so optional auth emails stay blank unless a valid email is actually provided.
    - Restored profile-avatar, inventory-image, supplier, and transaction-log saves using PocketBase-compatible file and field handling.
    - Added end-to-end persistence for the new PocketBase `date` fields on `logs_input` and `logs_output`, so imported Receive/Out dates are now stored correctly.
    - Added metadata persistence for imported log supplier, batch/lot, and description values so optional spreadsheet fields are preserved even when they are not part of the core PocketBase log schema.
- Dashboard, Navigation & Personalization:
    - Added per-user dashboard widget visibility controls and persisted those choices in user settings.
    - Brought the main navigation animation in line with the smoother Overview/Act tab transition style.
    - Improved fade and remount behavior for dashboard view switching, including the Critical Replenishment widget.
    - Centered the expanded Admin Dashboard tab bar and rebuilt it with a cleaner pill layout for large screens.
    - Rebuilt the main navigation into a modular shared component with a fluid desktop selector that matches the Overview and Act tab behavior while keeping the full tab labels visible.
    - Added a separate floating mobile bottom-navigation mode with a brighter translucent glass style that hides after scrolling down and reappears only when the user returns to the top.
    - Stabilized desktop navigation indicator positioning so switching between far-left and far-right tabs no longer breaks the icon-collapse animation.
    - Tightened desktop navigation and admin-tab container sizing so the bars no longer leave oversized empty gutters at either end.
- Warehouse Filtering & Responsive UI:
    - Fixed warehouse filters to use configured warehouse locations plus persisted item metadata, so missing inline fields no longer break the pill tabs.
    - Added a shared warehouse-pill component that truncates long location names cleanly, shows full names on hover, and scrolls smoothly on small screens.
    - Expanded small-screen layout support across navigation, dashboards, toolbars, tables, and modal content for better mobile usability.
    - Corrected mobile layout alignment issues in Admin Dashboard and User Settings where stacked controls and content blocks could drift out of place.
    - Updated the Inventory view switcher to use dedicated icons on mobile for Overview, Arrivals, and Shipments while keeping icon-plus-label tabs on desktop.
    - Extended the same responsive tab-switcher treatment to the Dashboard Overview and Act control for cleaner small-screen navigation.
- Import & Spreadsheet Sync:
    - Fixed quantity parsing so import no longer mistakes `Weight/Pc per QTY` columns for the real movement quantity column.
    - Updated spreadsheet import rules so the `Items` tab only contributes item-master fields, while stock on hand is recalculated from `Receive` minus `Out`.
    - Made supplier and batch/lot optional for imported logs while still preserving them whenever the spreadsheet provides those values.
    - Added automatic UoM collection updates when imported sheets contain valid units that do not yet exist in the database.
    - Corrected review-sync commit behavior so imported Receive and Out logs now replace the existing log sets instead of only appending new entries.
- InnoAssistant Expansion:
    - Extended local command handling to answer warehouse summaries, stock movement totals, supplier contact lookups, and top-mover queries.
    - Added direct item-state actions such as marking an item as Restocked or In Process without leaving the dashboard.
    - Introduced guided suggestion chips and clearer response cards to make the assistant faster to use for common workflows.
- Branding & Release Sync:
    - Switched favicon and About assets back to the existing `assets/images/icon.png`.
    - Updated the About panel and release metadata for version 0.13.2.

Version 0.13.3 - March 28, 2026
-------------------------------------------------------
- Authentication & Brand Identity Refinement:
    - UI/UX Aesthetics: Implemented a new premium, high-fidelity Login UI featuring edge-to-edge blurred backgrounds and sophisticated glassmorphism.
    - BackgroundManager Engine: Developed a dedicated `backgroundManager.js` utility to handle dynamic, performant background transitions and blur filtering.
    - Universal Auth Mode: Enabled "Username or Email" authentication, allowing users more flexibility when signing into the platform.
    - Branding & Layout: Enlarged corporate branding elements and enforced a static, unified copyright footer across the authentication gateway.
    - Global Theme Sync: Ensured consistent application of Administrator-defined global accent colors across all login and setup components.

Version 0.13.4 - March 28, 2026
-------------------------------------------------------
- Mobile Navigation Fix (Critical):
    - Identified and resolved a Chrome-specific bug where `backdrop-filter` on the top brand bar created a new containing block for `position: fixed` child elements, trapping the mobile bottom navigation inside the header instead of pinning it to the viewport bottom.
    - Applied `backdrop-filter: none` at the ≤680px breakpoint to restore correct fixed positioning of the bottom nav without affecting the desktop glass effect.
- Sticky Header Architecture:
    - Established a proper full-height scroll container chain (`html → body → #root → app-wrapper → app-layout`) so that `position: sticky` on the top brand bar is respected at all viewport widths.
    - Added `#root` flex layout rules and `html, body { height: 100% }` to constrain scrolling exclusively to `app-layout`, preventing the document body from acting as the scroll target.
- Admin Dashboard Tab Bar Redesign:
    - Rebuilt the desktop tab bar as a single-row horizontal pill strip (consistent with the main app navigation style), replacing the old multi-row wrapping grid.
    - Redesigned the mobile (≤680px) tab bar as a clean vertical stacked list — each tab is full-width with left-aligned icon and label, separated by subtle dividers, matching the intended mobile UX.
    - Removed the legacy `min-width: 152px` constraint and `flex-wrap: wrap` behavior that caused tabs to spill into uneven multi-row layouts at intermediate widths.

Version 0.14.0 - March 30, 2026
-------------------------------------------------------
- Assets & Partners UI Refactoring:
    - Re-engineered the industrial control hierarchy: Warehouse filters moved to the top-level, followed by tab switchers and circular display toggles.
    - Implemented a more aggressive high-density layout by reducing vertical whitespace across Hero sections, KPI cards, and Management toolbars.
    - Widened the search bar for improved usability and accessibility.
- Admin Dashboard Architecture:
    - Resolved critical "Element type is invalid" runtime errors by correcting component scope and initialization for OverviewTab and Icons.
    - Integrated a fully responsive DashboardGrid that dynamically adjusts to container size and supports persistent widget reordering.
    - Consolidated the backend architecture by unifying state management and stripping redundant initialization logic.
- Data Import & Synchronization:
    - Finalized the "Master Sync Engine" with full support for bulk data reconciliation and error handling.
    - Implemented a GitHub-style visual diff review interface within the ManageDataTab for auditing spreadsheet imports before committing.
    - Restored missing Export and Data Health panels to the main synchronization interface.
- Navigation & Brand Identity:
    - Polished the modular ERP sidebar with consistent visual branding and intuitive state transitions (expanded/collapsed).
    - Hardened responsive navigation logic for mobile and small-screen devices.

Version 0.14.1 - March 30, 2026
-------------------------------------------------------
- Admin Dashboard Stability & Bug Fixes:
    - Resolved persistent "Element type is invalid" runtime error by correcting component scoping and initialization timing.
    - Elevated `Icons` to file-level scope in `adminDashboard.js` to ensure all internal sub-components have consistent access to global assets.
    - Implemented defensive rendering in the Admin Shell to gracefully handle script execution race conditions with an "Initializing..." state and loading spinner.
    - Added `/** @jsx React.createElement */` pragma across core components to ensure standard Babel standalone compilation and prevent JSX transform mismatches.

Version 0.14.2 - March 30, 2026
-------------------------------------------------------
- VPS Compatibility & Initialization Stability:
    - Fixed a critical race condition where components would attempt to access `Icons` or `AdminDashboard` before their respective scripts had fully initialized.
    - Moved all global icon extractions from the file-level top scope into local component function scopes, ensuring total initialization safety on high-latency network connections.
    - Added comprehensive null-safe fallbacks (`window.Icons || {}`) across all shared and administrative modules to prevent "got: undefined" React render crashes.
    - Hardened the `AdminApp` entry point with a robust synchronization check before mounting any administrative sub-components.
- Implemented "Bulletproof" Initialization:
    - Added a mandatory global dependency check in admin.html before React mounting.
    - Tier 2 protection: Added a Proxy-based failsafe for Icons to prevent "Element type is invalid" crashes.

Version 0.15.0 - March 30, 2026
-------------------------------------------------------
- Role-Based Access Control (RBAC) System:
    - Implemented a granular permission model based on the `user.restrictions` JSON array.
    - Dynamic route guards injected into `getPathView()` to prevent unauthorized hub navigation.
    - Automatic UI logic to filter restricted navigation tabs (Dashboard, Assets, Partners).
    - Administrative "Hub Access" panel for positive permission granting at a per-user level.
    - Unified "Revoked Actions" panel for disabling Add/Edit/Remove across all non-admin roles.
    - Comprehensive `AVAILABLE_ROLES` system to support custom roles (Manager, Sales, etc.).
- Infrastructure & Offline Resilience:
    - Migrated all external CDN dependencies (React, Babel, PocketBase, SheetJS) to local vendor assets.
    - Decoupled the platform from public internet requirements, enabling private intranet deployments.
- UI & Code Consolidation:
    - Refactored the "Unified Action Bar" into a modular shared component (`src/components/shared/layout.js`).
    - Consolidated search, sorting, and display toggles into the new `ActionBar` across all relevant hubs.
    - Merged `tmp_partners.js` into the main `partners.js` codebase and cleared technical debt.
    - Sanitized critical core files (app.js) with UTF-8 encoding and standardized line endings.
- Asset & Logic Modernization:
    - Re-engineered the icon system to load SVG assets directly from the filesystem via `Icons.js`.
    - Deprecated 500+ legacy React-wrapped icon components for high-performance native SVG fetching.
    - Optimized Hero filter rails with high-density pill selectors for location and asset visibility.
- Stability & Error Handling:
    - Hardened the `AdminDashboard` mounting sequence with strict global dependency validation.
    - Implemented a Proxy-based failsafe for `Icons` to ensure smooth rendering on missing icon requests.

Version 0.16.0 - April 6, 2026
-------------------------------------------------------
- Core Logic & Data Integration:
    - Fully implemented the "Customers" collection within the Partner Management system.
    - Integrated Customer CRUD operations into the global application state and `inventoryDataManager`.
    - Enhanced `Assets` view with contextual partner badging (Suppliers for materials, Customers for products).
    - Established bi-directional item linking for both suppliers and customers.
- User Experience & Interface:
    - Refactored `Partners` Hub with a dual-channel management interface (Suppliers/Customers tabs).
    - Improved item-linking workflow with a unified, searchable, and category-aware prompting system.
    - Standardized management toolbars across Partners and Assets for better UI consistency.
    - Added "Item Removed" and other interaction-specific confirmation toasts.
- Data Management & Synchronization:
    - Updated `inventory` schema to support multi-partner relationships.
    - Fixed real-time stock level synchronization for input and output transactions.
    - Repaired various JSX syntax and nesting errors in major view components.
    - Fixed "Database Integrity" panel in the overview tab of admin dashboard, where the percentage and score is removed.
    - Implemented "Export All" functionality for Input Logs and Output Logs, still based on the Frappe Guidelines for ERPNext.
    - The factory data reset will now reset specific parts of the database, like inventory master, input and output logs, suppliers, and customers.
- Data Management & Stability:
    - Resolved a critical data integrity bug where partial stock updates could lead to unintended record deletions and "forever" sync hangs.
    - Expanded the PocketBase synchronization engine to fully support `batch` and `date` fields for all transaction logs.
    - Optimized the `saveData` core logic to strictly distinguish between incremental updates and full deletions.
- UI/UX & Form Modernization:
    - Re-engineered the Item Management form: converted Category into a dynamic dropdown with "Add New" capability.
    - Streamlined the inventory prompt by removing redundant auto-calculated status fields.
    - Polished the transaction logging interface with dedicated fields for Batch, Date, and internal notes.
    - Corrected the labeling for Supplier Partners to improve field clarity.
- Role-Based Access Control (RBAC) System:
    - Finalized Global Role Access Settings allowing administrators to configure platform-wide hub permissions for all User Roles (Administrator, Manager, WarehouseStaff, Sales, Auditor).
    - Implemented a "Deferred Save" mechanism for Role settings, requiring a "Done" confirmation before persisting changes to the database.
    - Integrated automatic access-level derivation for individual users based on their assigned role, with an "Administrator" lock to prevent accidental lockout.
    - Restricted "Revoked Actions" visibility solely to the "Auditor" mapping, keeping the interface clean for other roles.
- Dashboard Widget Visibility & Personalization:
    - Engineered a new "Eye-Off" (Hide) interaction for all Dashboard and Admin widgets, allowing users to customize their views directly from the grid.
    - Developed a unified "Widget Visibility Controls" panel that stays perfectly synchronized with on-card hide buttons.
    - Implemented support for "Grouped Visibility" (e.g., hiding the Metrics card toggles all sub-meters like Low Stock and Partners simultaneously).
    - Added a "Restore All" recovery function to the Admin Overview to reset hidden widgets instantly.
    - Optimized the Dashboard Grid to handle external visibility state, preventing "ghosting" artifacts where hidden items would fail to reappear.
- UI/UX & Component Standardization:
    - Refactored the Admin Overview toolbar to include Rearrange Mode and Role Access Settings.
    - Modernized the widget control buttons with integrated status icons (Eye/Eye-Off) and distinct active/inactive states.
    - Switched all layout and visibility state updates to functional setters, ensuring zero-latency responsiveness during rapid UI interactions.
- Icon System & Fallbacks:
    - Integrated a suite of built-in SVG icons (Undo, Redo, Question) to ensure zero-latency loading and reliable 404-safe fallbacks.
    - Set the built-in "Question" mark as the global default for unmapped or missing assets.

Version 0.16.1 (April 6, 2026)
-------------------------------------------------------
- Security & Data Consistency (Cache Busting):
    - Implemented a mandatory Version-Locked Cache Invalidation system in `coreDataHandler.js`.
    - The platform now automatically detects when a new version is deployed and clears stale `localStorage` data associated with previous builds.
    - Synchronized the internal `VERSION` constant (0.16.1) across the core data handler and UI settings to ensure consistent version reporting.
- Infrastructure & Deployment Stability:
    - Resolved a critical "Ghost Data" issue where client-side caches could conflict with live database records on high-latency VPS deployments.
    - Optimized the initial loading sequence to perform an early version verification before mounting the main application state.
- UI/UX Refinement:
    - Updated version indicators in User Settings and Admin Overview to reflect build 0.16.1.
    - Standardized copyright and author labels across all administrative and user-facing modals.

Version 0.16.2 (April 6, 2026)
-------------------------------------------------------
- Smart Automation & Intelligence:
    - Re-engineered the automation engine to detect complex operational issues including stock-out risks, unlinked items, and stagnant stock.
    - Added an actionable Urgent Actions list for direct one-click replenishment and dispatch handling.
- InnoAssistant Engine Expansion:
    - Expanded NLP capabilities to handle broader warehouse, inventory, and partner-specific queries.
    - Added support for direct item state modifications and executable commerce commands.
- Dashboard & Widget Modernization:
    - Redesigned Warehouse Health and Category Performance widgets with descriptive analytics.
    - Integrated the new Smart Automation hub as a central dashboard component.
    - Added Warehouse Health toggle to the rearrange mode visibility panel.
- Smart Automation Consistency Improvements:
    - Synchronized item-level warnings in Assets and Partners views with the core automation engine.
    - Updated the AutomationRibbon to represent all 5 intelligence categories (Critical, Forecast, Dispatch, Setup, Dormant).
    - Integrated smart engine insights directly into the Product Statistics summary panel.
    - Added actionable automation badges to both card and table views across the application.
- Login UI & Visual Polish:
    - Implemented a premium sequential background cycling system (backgrounds 1 through 4) on the login screen.
    - Resolved a background-skipping issue caused by React StrictMode double-mounting by isolating the increment logic to a one-time useEffect hook.
- Data Management & Performance Optimization:
    - Optimized the `saveData` core logic in `inventoryDataManager.js` with `Promise.all` parallelization, enabling high-speed bulk CRUD operations.
    - Implemented Request Deduplication (shared promises) in `coreDataHandler.js` to prevent redundant simultaneous network calls during app load.
    - Added a 500ms debounced persistence mechanism for the local cache to eliminate UI stutters during large-scale data updates.
    - Increased the system metadata cache TTL to 10 seconds to significantly reduce redundant network polling for 'lastUpdated' maps.
    - Fixed a critical bug in handlePromptConfirm to support direct assistant-driven item edits.
    - Synchronized application version to 0.16.2 across all shared modules.
- General Stability & State Management:
    - Fixed a critical bug in `handlePromptConfirm` to support direct assistant-driven item edits.
    - Synchronized application version to 0.16.2 and updated internal constants in `coreDataHandler.js` to ensure consistent cache invalidation.

Version 0.16.3 (April 8, 2026)
-------------------------------------------------------
- PocketBase Authentication & Password Recovery:
    - Successfully migrated the "Reset Password" feature from Firebase-compat to native PocketBase logic.
    - Configured PocketBase to use Mailtrap SMTP (Port 2525) to bypass Contabo VPS outbound port restrictions.
    - Implemented a modern, responsive HTML email template for password resets featuring glassmorphic buttons and gradient aesthetics.
    - Integrated `{ACTION_URL}` and `{APP_URL}` validation fixes for local and production VPS environments.
- UI/UX & Parameter Stability:
    - Engineered a "Bulletproof" URL parameter parser in `AuthView` that handles query strings in both standard and hash-based routing (`/#/...`).
    - Implemented a session-guard bypass in `App.js` to allow unauthorized users to remain on the password reset screen without being redirected to login.
    - Fixed a critical "Email not found" bug by implementing a username-to-email lookup bridge for forgotten password requests.
- General Performance & Infrastructure:
    - Updated global `VERSION` constant to 0.16.3 to trigger build-critical cache invalidation.
    - Optimized VPS-specific auth redirects to ensure reliability on high-latency DuckDNS connections.
    - Cleaned up duplicate activity log triggers in `AuthDataManager` for better audit trail accuracy.

Version 0.16.4 (April 28, 2026)
-------------------------------------------------------
- Migration to Local-Only Architecture:
    - Removed all PocketBase VPS dependencies to support full local execution.
    - Implemented a browser-based persistent cache system via `localStorage` for all inventory and system data.
    - Optimized system memory by removing the PocketBase SDK from the vendor bundle.
- Security & Environment Updates:
    - Replaced `pocketbase_config.json` with a secure `.env` configuration system.
    - Created '.gitignore' to ignore .env and other unnecessary files.
    - Implemented high-security session management: `localStorage` is automatically cleared on logout and login page load.
- Guest Mode Implementation:
    - Added a hardcoded administrative account for immediate local access (`default` / `cb_demo_2026`).
    - Configured the default user to operate in an ephemeral "Guest Mode" where session changes are temporary.
    - Disabled remote authentication features (signup, password recovery) which are inapplicable in local-only mode.
- System Stability & Performance:
    - Fixed critical initialization race conditions in the core data handler.
    - Synchronized application version to 0.16.4 across all shared modules and UI components.
    - Cleaned up redundant network calls previously used for PocketBase heartbeat checks.

Version 0.16.5 (April 28, 2026)
-------------------------------------------------------
- Deployment Optimization & Infrastructure:
    - Successfully deployed the application to Vercel for cloud-based accessibility.
    - Migrated all major library dependencies (React, React DOM, Babel, TensorFlow, Compromise, SheetJS) to CDN-based loading (unpkg.com).
    - Resolved 404 resource errors on deployment by bypassing `.gitignore` restrictions via CDN integration.
    - Upgraded core libraries to production-optimized builds, significantly reducing initial page load weight and improving runtime security.
- Version Synchronization:
    - Synchronized application version to 0.16.5 across the core data handler, user settings, and documentation.
    - Updated cache-busting version strings in HTML entry points to ensure immediate delivery of the latest assets.
